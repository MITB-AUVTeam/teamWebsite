# Project-Scoped Rules

## Git Push Constraint
- **Do not run `git push` or push any code changes to the remote repository under any circumstances**, unless the user explicitly tells you to. All modifications should remain locally staged in the workspace.
